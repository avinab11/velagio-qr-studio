# 1. PRIORITY: Dynamic QR Resolver (Captures the ID and sends it to your function)
/q/* /.netlify/functions/resolve?id=:splat 200!

# 2. BRAND MOVE: Redirects old Netlify traffic to your new custom domain
https://velagiofreeqr.netlify.app/* https://www.velagiofreeqr.com/:splat 301!

# 3. SPA FALLBACK: Ensures the rest of your app (blog, etc.) loads correctly
/* /index.html 200
